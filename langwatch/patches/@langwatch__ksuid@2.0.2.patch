diff --git a/dist/index.cjs b/dist/index.cjs
index 60d572160a4c8f47091d805b99b43120a18bb435..2d368dfc50faa8a0d1ef2d0c0b49f3e20454a0f9 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -301,18 +301,18 @@ var Ksuid = class _Ksuid {
     const decoded = new Uint8Array(DECODED_LEN);
     decoded[0] = 0;
     decoded[1] = 0;
-    decoded[2] = this._timestamp >> 40 & 255;
-    decoded[3] = this._timestamp >> 32 & 255;
-    decoded[4] = this._timestamp >> 24 & 255;
-    decoded[5] = this._timestamp >> 16 & 255;
-    decoded[6] = this._timestamp >> 8 & 255;
-    decoded[7] = this._timestamp & 255;
+    let timestamp = BigInt(this._timestamp);
+    for (let i = 7; i >= 2; i--) {
+      decoded[i] = Number(timestamp & 255n);
+      timestamp >>= 8n;
+    }
     const instanceBuffer = this._instance.toBuffer();
     decoded.set(instanceBuffer, 8);
-    decoded[17] = this._sequenceId >> 24 & 255;
-    decoded[18] = this._sequenceId >> 16 & 255;
-    decoded[19] = this._sequenceId >> 8 & 255;
-    decoded[20] = this._sequenceId & 255;
+    let sequenceId = BigInt(this._sequenceId);
+    for (let i = 20; i >= 17; i--) {
+      decoded[i] = Number(sequenceId & 255n);
+      sequenceId >>= 8n;
+    }
     const encoded = encode(decoded);
     const padded = encoded.padStart(ENCODED_LEN, "0");
     this._string = prefix + padded;
@@ -344,10 +344,29 @@ var Ksuid = class _Ksuid {
     if (fullDecoded[0] !== 0 || fullDecoded[1] !== 0) {
       throw new Error("Timestamp greater than 8921556-12-07T10:44:16Z");
     }
-    const timestamp = fullDecoded[2] << 40 | fullDecoded[3] << 32 | fullDecoded[4] << 24 | fullDecoded[5] << 16 | fullDecoded[6] << 8 | fullDecoded[7];
+    let timestamp = 0n;
+    for (let i = 2; i <= 7; i++) {
+      timestamp = timestamp << 8n | BigInt(fullDecoded[i]);
+    }
+    let legacyTimestamp = 0n;
+    for (let i = 4; i <= 7; i++) {
+      legacyTimestamp = legacyTimestamp << 8n | BigInt(fullDecoded[i]);
+    }
+    if (fullDecoded[2] === fullDecoded[6] && fullDecoded[3] === fullDecoded[7] && legacyTimestamp <= 2147483647n) {
+      timestamp = legacyTimestamp;
+    }
     const instance = Instance.fromBuffer(fullDecoded.slice(8, 17));
-    const sequenceId = fullDecoded[17] << 24 | fullDecoded[18] << 16 | fullDecoded[19] << 8 | fullDecoded[20];
-    return new _Ksuid(environment, resource, timestamp, instance, sequenceId);
+    let sequenceId = 0n;
+    for (let i = 17; i <= 20; i++) {
+      sequenceId = sequenceId << 8n | BigInt(fullDecoded[i]);
+    }
+    return new _Ksuid(
+      environment,
+      resource,
+      Number(timestamp),
+      instance,
+      Number(sequenceId)
+    );
   }
   /**
    * Compares this KSUID with another for equality
@@ -497,11 +516,14 @@ var Node = class {
   generate(resource) {
     checkNonEmptyString("resource", resource);
     const now = Math.floor(Date.now() / 1e3);
-    if (this._lastTimestamp === now) {
-      this._currentSequence += 1;
-    } else {
+    if (now > this._lastTimestamp) {
       this._lastTimestamp = now;
       this._currentSequence = 0;
+    } else if (this._currentSequence === 4294967295) {
+      this._lastTimestamp += 1;
+      this._currentSequence = 0;
+    } else {
+      this._currentSequence += 1;
     }
     return this._ksuidFactory(
       this._environment,
diff --git a/dist/index.js b/dist/index.js
index 59c134400e99fd31261000f104001ce0386aabb8..39b00e5fba5d9d9231ad541f62ff7e5dd3c757ed 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -299,18 +299,18 @@ var Ksuid = class _Ksuid {
     const decoded = new Uint8Array(DECODED_LEN);
     decoded[0] = 0;
     decoded[1] = 0;
-    decoded[2] = this._timestamp >> 40 & 255;
-    decoded[3] = this._timestamp >> 32 & 255;
-    decoded[4] = this._timestamp >> 24 & 255;
-    decoded[5] = this._timestamp >> 16 & 255;
-    decoded[6] = this._timestamp >> 8 & 255;
-    decoded[7] = this._timestamp & 255;
+    let timestamp = BigInt(this._timestamp);
+    for (let i = 7; i >= 2; i--) {
+      decoded[i] = Number(timestamp & 255n);
+      timestamp >>= 8n;
+    }
     const instanceBuffer = this._instance.toBuffer();
     decoded.set(instanceBuffer, 8);
-    decoded[17] = this._sequenceId >> 24 & 255;
-    decoded[18] = this._sequenceId >> 16 & 255;
-    decoded[19] = this._sequenceId >> 8 & 255;
-    decoded[20] = this._sequenceId & 255;
+    let sequenceId = BigInt(this._sequenceId);
+    for (let i = 20; i >= 17; i--) {
+      decoded[i] = Number(sequenceId & 255n);
+      sequenceId >>= 8n;
+    }
     const encoded = encode(decoded);
     const padded = encoded.padStart(ENCODED_LEN, "0");
     this._string = prefix + padded;
@@ -342,10 +342,29 @@ var Ksuid = class _Ksuid {
     if (fullDecoded[0] !== 0 || fullDecoded[1] !== 0) {
       throw new Error("Timestamp greater than 8921556-12-07T10:44:16Z");
     }
-    const timestamp = fullDecoded[2] << 40 | fullDecoded[3] << 32 | fullDecoded[4] << 24 | fullDecoded[5] << 16 | fullDecoded[6] << 8 | fullDecoded[7];
+    let timestamp = 0n;
+    for (let i = 2; i <= 7; i++) {
+      timestamp = timestamp << 8n | BigInt(fullDecoded[i]);
+    }
+    let legacyTimestamp = 0n;
+    for (let i = 4; i <= 7; i++) {
+      legacyTimestamp = legacyTimestamp << 8n | BigInt(fullDecoded[i]);
+    }
+    if (fullDecoded[2] === fullDecoded[6] && fullDecoded[3] === fullDecoded[7] && legacyTimestamp <= 2147483647n) {
+      timestamp = legacyTimestamp;
+    }
     const instance = Instance.fromBuffer(fullDecoded.slice(8, 17));
-    const sequenceId = fullDecoded[17] << 24 | fullDecoded[18] << 16 | fullDecoded[19] << 8 | fullDecoded[20];
-    return new _Ksuid(environment, resource, timestamp, instance, sequenceId);
+    let sequenceId = 0n;
+    for (let i = 17; i <= 20; i++) {
+      sequenceId = sequenceId << 8n | BigInt(fullDecoded[i]);
+    }
+    return new _Ksuid(
+      environment,
+      resource,
+      Number(timestamp),
+      instance,
+      Number(sequenceId)
+    );
   }
   /**
    * Compares this KSUID with another for equality
@@ -495,11 +514,14 @@ var Node = class {
   generate(resource) {
     checkNonEmptyString("resource", resource);
     const now = Math.floor(Date.now() / 1e3);
-    if (this._lastTimestamp === now) {
-      this._currentSequence += 1;
-    } else {
+    if (now > this._lastTimestamp) {
       this._lastTimestamp = now;
       this._currentSequence = 0;
+    } else if (this._currentSequence === 4294967295) {
+      this._lastTimestamp += 1;
+      this._currentSequence = 0;
+    } else {
+      this._currentSequence += 1;
     }
     return this._ksuidFactory(
       this._environment,
